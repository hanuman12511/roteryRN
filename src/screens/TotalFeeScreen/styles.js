import {View, StyleSheet, ScrollView} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f2f1f1',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  totalFeeDetailSection: {
    flex: 1,
    marginHorizontal: 8,
    marginTop: 8,
  },
  detailContainer: {
    backgroundColor: '#fff',
    borderLeftColor: '#1ba2de',
    borderLeftWidth: 3,
    borderRadius: 2,
    marginBottom: 8,
  },
});
